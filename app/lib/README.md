# Mã nguồn Dart

Mã nguồn được chia thành `app`, `frontend`, `backend` và `shared`; `main.dart` là điểm khởi chạy.

`frontend` gọi `backend` thông qua contract; `shared` chứa model và contract dùng chung; backend không phụ thuộc vào widget Flutter.
